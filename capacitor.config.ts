import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.alarmaRobo.lcea',
  appName: 'alarma-de-robo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
